-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/tests/fixtures/advanced/template-literals.ts

import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions
import TSLean.Runtime.Monad

open TSLean

namespace TSLean.Generated.TemplateLiterals

-- // Template literals → s!"..." interpolation
def greeting (name : String) (age : Float) : String :=
  s!"Hello, {name}! You are {age} years old."

def url (base : String) (path : String) (id : String) : String :=
  s!"{base}/{path}/{id}"

def jsonLike (key : String) (value : String) : String :=
  s!"\{"{key}": "{value}"}"

def multipart (a : String) (b : String) (c : Float) : String :=
  s!"{a}-{b}={c}"

def endpoint : String := "https://api.example.com"

def userUrl (userId : String) : String :=
  s!"{endpoint}/users/{userId}"

def logMessage (level : String) (msg : String) : IO String :=
  do
    let ts : Float := IO.monoNanoseconds
      pure (s!"[{level}] {msg} at {ts}")

structure User where
  mk ::
  id : String
  name : String
  deriving Repr, BEq

def userTag (u : User) : String :=
  s!"<user id="{u.id}">{u.name}</user>"

end TSLean.Generated.TemplateLiterals