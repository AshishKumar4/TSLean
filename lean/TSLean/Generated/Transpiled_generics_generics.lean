-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/tests/fixtures/generics/generics.ts

import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions

open TSLean

namespace TSLean.Generated.Generics

-- // Generics: type parameters, higher-kinded patterns
def identity {T : Type} (x : T) : T :=
  x

def constant {T : Type} {U : Type} (x : T) : U → T :=
  fun _u => x

def compose {A : Type} {B : Type} {C : Type} (f : A → B) (g : B → C) : A → C :=
  fun a => g (f a)

structure Pair (A : Type) (B : Type) where
  mk ::
  first : A
  second : B
  deriving Repr, BEq

def makePair {A : Type} {B : Type} (a : A) (b : B) : Pair A B :=
  { first := a, second := b }

def swapPair {A : Type} {B : Type} (p : Pair A B) : Pair B A :=
  { first := p.second, second := p.first }

def mapOpt {T : Type} {U : Type} (opt : Option T) (f : T → U) : Option U :=
  match opt with
    | none => none
    | some _v => f _v

def flatMapOpt {T : Type} {U : Type} (opt : Option T) (f : T → Option U) : Option U :=
  match opt with
    | none => none
    | some _v => f _v

end TSLean.Generated.Generics