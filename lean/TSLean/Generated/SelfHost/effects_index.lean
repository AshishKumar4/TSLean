-- Auto-generated by ts-lean-transpiler (stub for compilation)
-- Source: /workspace/tslean/src/effects/index.ts

import TSLean.Generated.SelfHost.ir_types
import TSLean.External.Typescript
import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions
import TSLean.Stdlib.HashMap

open TSLean TSLean.Generated.Types TSLean.External.Typescript

namespace TSLean.Generated.SelfHost.Effects

def inferNodeEffect (node : Node) (checker : TypeChecker) : Effect := .Pure

def monadString (effect : Effect) (stateTypeName : String := "σ") : String :=
  match effect with
  | .Pure => "Id"
  | .IO | .Async => "IO"
  | .State _ => s!"StateT {stateTypeName} IO"
  | .Except _ => "ExceptT TSError IO"
  | .Combined _ => s!"StateT {stateTypeName} (ExceptT TSError IO)"

def doMonadType (stateTypeName : String) : String :=
  s!"StateT {stateTypeName} (ExceptT TSError IO)"

def joinEffects (a b : Effect) : Effect :=
  match a, b with
  | .Pure, e | e, .Pure => e
  | _, _ => .Combined #[a, b]

def isAssignOp (kind : SyntaxKind) : Bool :=
  kind == .Other 0  -- placeholder

def isIncrDecr (kind : SyntaxKind) : Bool :=
  kind == .Other 0  -- placeholder

end TSLean.Generated.SelfHost.Effects
