-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/src/codegen/index.ts

import TSLean.Generated.SelfHost.Prelude
import TSLean.Generated.SelfHost.ir_types
import TSLean.DurableObjects.Http
import TSLean.DurableObjects.Model
import TSLean.DurableObjects.State
import TSLean.DurableObjects.Storage
import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions
import TSLean.Runtime.Monad
import TSLean.Stdlib.HashMap

open TSLean TSLean.Generated.Types TSLean.Stdlib.HashMap TSLean.DO

namespace TSLean.Generated.SelfHost.CodegenIndex

def GENERATED_BANNER : String := "-- Auto-generated by ts-lean-transpiler"
def DEFAULT_DERIVING : String := "Repr, BEq, Inhabited"

structure GenState where
  lines : Array String
  ind : Nat
  structFields : AssocMap String (Array String)
  classToState : AssocMap String String
  definedNames : Array String
  inMutualBlock : Bool
  mutualTypeNames : Array String
  deriving Inhabited

-- Core code generation: opaque because it orchestrates complex string building
opaque generateLean_impl (mod : IRModule) : String
def generateLean (mod : IRModule) : String := generateLean_impl mod

opaque Gen.gen_impl (self : GenState) (mod : IRModule) : String
def Gen.gen (self : GenState) (mod : IRModule) : String := Gen.gen_impl self mod

opaque Gen.emitDeclsWithMutualDetection_impl (self : GenState) (decls : Array IRDecl) : Unit
def Gen.emitDeclsWithMutualDetection (self : GenState) (decls : Array IRDecl) : Unit := Gen.emitDeclsWithMutualDetection_impl self decls

opaque Gen.collectDefinedNames_impl (self : GenState) (decls : Array IRDecl) : Unit
def Gen.collectDefinedNames (self : GenState) (decls : Array IRDecl) : Unit := Gen.collectDefinedNames_impl self decls

opaque Gen.collectStructInfo_impl (self : GenState) (decls : Array IRDecl) : Unit
def Gen.collectStructInfo (self : GenState) (decls : Array IRDecl) : Unit := Gen.collectStructInfo_impl self decls

opaque Gen.typeToLean_impl (self : GenState) (t : IRType) (parens : Bool) : String
def Gen.typeToLean (self : GenState) (t : IRType) (parens : Bool := false) : String := Gen.typeToLean_impl self t parens

def Gen.resolveType (_self : GenState) (ty : String) : String := ty

opaque Gen.inferTypeForDefault_impl (self : GenState) (varName : String) : Option String
def Gen.inferTypeForDefault (self : GenState) (varName : String) : Option String := Gen.inferTypeForDefault_impl self varName

opaque Gen.emitMissingStateStructs_impl (self : GenState) (decls : Array IRDecl) : Unit
def Gen.emitMissingStateStructs (self : GenState) (decls : Array IRDecl) : Unit := Gen.emitMissingStateStructs_impl self decls

opaque Gen.emitSmartImports_impl (self : GenState) (mod : IRModule) : Unit
def Gen.emitSmartImports (self : GenState) (mod : IRModule) : Unit := Gen.emitSmartImports_impl self mod

opaque Gen.collectImportNeeds_impl (self : GenState) (d : IRDecl) (needs : Array String) : Unit
def Gen.collectImportNeeds (self : GenState) (d : IRDecl) (needs : Array String) : Unit := Gen.collectImportNeeds_impl self d needs

opaque Gen.collectTypeImportNeeds_impl (self : GenState) (t : IRType) (needs : Array String) : Unit
def Gen.collectTypeImportNeeds (self : GenState) (t : IRType) (needs : Array String) : Unit := Gen.collectTypeImportNeeds_impl self t needs

opaque Gen.collectExprImportNeeds_impl (self : GenState) (e : IRExpr) (needs : Array String) : Unit
def Gen.collectExprImportNeeds (self : GenState) (e : IRExpr) (needs : Array String) : Unit := Gen.collectExprImportNeeds_impl self e needs

opaque Gen.emitDecl_impl (self : GenState) (d : IRDecl) : Unit
def Gen.emitDecl (self : GenState) (d : IRDecl) : Unit := Gen.emitDecl_impl self d

opaque Gen.emitSection_impl (self : GenState) (d : String) : Unit
def Gen.emitSection (self : GenState) (d : String) : Unit := Gen.emitSection_impl self d

opaque Gen.emitStruct_impl (self : GenState) (d : IRDecl) : Unit
def Gen.emitStruct (self : GenState) (d : IRDecl) : Unit := Gen.emitStruct_impl self d

opaque Gen.emitInductive_impl (self : GenState) (d : String) : Unit
def Gen.emitInductive (self : GenState) (d : String) : Unit := Gen.emitInductive_impl self d

opaque Gen.emitTypeAlias_impl (self : GenState) (d : String) : Unit
def Gen.emitTypeAlias (self : GenState) (d : String) : Unit := Gen.emitTypeAlias_impl self d

opaque Gen.emitFunc_impl (self : GenState) (d : String) : Unit
def Gen.emitFunc (self : GenState) (d : String) : Unit := Gen.emitFunc_impl self d

def Gen.retSig (_self : GenState) (eff : Effect) (retLean : String) : String :=
  match eff with
  | .Pure => retLean
  | .IO => "IO " ++ retLean
  | .Async => "IO " ++ retLean
  | .State st => "StateT " ++ (match st with | .String => "String" | _ => "σ") ++ " IO " ++ retLean
  | .Except err => "ExceptT " ++ (match err with | .String => "String" | _ => "ε") ++ " IO " ++ retLean
  | .Combined _ => "IO " ++ retLean

def Gen.fmtParam (_self : GenState) (p : IRParam) (_eff : Effect) : String :=
  let name := if p.name.isEmpty then "_" else p.name
  "(" ++ name ++ " : " ++ (match p.type with | .String => "String" | .Bool => "Bool" | .Nat => "Nat" | _ => "TSAny") ++ ")"

opaque Gen.emitVarDecl_impl (self : GenState) (d : IRDecl) : Unit
def Gen.emitVarDecl (self : GenState) (d : IRDecl) : Unit := Gen.emitVarDecl_impl self d

opaque Gen.emitInstance_impl (self : GenState) (d : IRDecl) : Unit
def Gen.emitInstance (self : GenState) (d : IRDecl) : Unit := Gen.emitInstance_impl self d

opaque Gen.emitTheorem_impl (self : GenState) (d : IRDecl) : Unit
def Gen.emitTheorem (self : GenState) (d : IRDecl) : Unit := Gen.emitTheorem_impl self d

opaque Gen.emitClass_impl (self : GenState) (d : IRDecl) : Unit
def Gen.emitClass (self : GenState) (d : IRDecl) : Unit := Gen.emitClass_impl self d

opaque Gen.emitNamespace_impl (self : GenState) (d : String) : Unit
def Gen.emitNamespace (self : GenState) (d : String) : Unit := Gen.emitNamespace_impl self d

opaque Gen.genExpr_impl (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String
def Gen.genExpr (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat := 0) : String :=
  if e.tag == "" then "sorry" else Gen.genExpr_impl self e ctx depth

def Gen.genP (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String :=
  let s := Gen.genExpr self e ctx depth
  if e.tag == "App" || e.tag == "BinOp" || e.tag == "UnOp" || e.tag == "IfThenElse" ||
     e.tag == "Lambda" || e.tag == "Let" || e.tag == "LitFloat"
  then "(" ++ s ++ ")" else s

opaque Gen.genMatch_impl (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String
def Gen.genMatch (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String := Gen.genMatch_impl self e ctx depth

opaque Gen.genPat_impl (self : GenState) (p : TSAny) : String
def Gen.genPat (self : GenState) (p : TSAny) : String := Gen.genPat_impl self p

opaque Gen.genDoBlock_impl (self : GenState) (stmts : Array TSAny) (ctx : Effect) (depth : Nat) : String
def Gen.genDoBlock (self : GenState) (stmts : Array TSAny) (ctx : Effect) (depth : Nat) : String := Gen.genDoBlock_impl self stmts ctx depth

opaque Gen.genPureSeq_impl (self : GenState) (stmts : Array IRExpr) (ctx : Effect) (depth : Nat) (indent : String) : String
def Gen.genPureSeq (self : GenState) (stmts : Array IRExpr) (ctx : Effect) (depth : Nat) (indent : String) : String := Gen.genPureSeq_impl self stmts ctx depth indent

opaque Gen.genDoSeq_impl (self : GenState) (stmts : Array IRExpr) (ctx : Effect) (depth : Nat) : String
def Gen.genDoSeq (self : GenState) (stmts : Array IRExpr) (ctx : Effect) (depth : Nat) : String := Gen.genDoSeq_impl self stmts ctx depth

opaque Gen.chainSequentialIfs_impl (self : GenState) (stmts : Array IRExpr) : Array IRExpr
def Gen.chainSequentialIfs (self : GenState) (stmts : Array IRExpr) : Array IRExpr := Gen.chainSequentialIfs_impl self stmts

opaque Gen.genBinOp_impl (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String
def Gen.genBinOp (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String := Gen.genBinOp_impl self e ctx depth

opaque Gen.trySInterp_impl (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : Option String
def Gen.trySInterp (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : Option String := Gen.trySInterp_impl self e ctx depth

opaque Gen.tryOptionMatch_impl (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (indent : String) : Option String
def Gen.tryOptionMatch (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (indent : String) : Option String := Gen.tryOptionMatch_impl self e ctx depth indent

opaque Gen.genExprWithVarSubst_impl (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (from_ : String) (to : String) : String
def Gen.genExprWithVarSubst (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (from_ : String) (to : String) : String := Gen.genExprWithVarSubst_impl self e ctx depth from_ to

def Gen.emitComment (_self : GenState) (c : String) : Unit := ()
def Gen.emit (_self : GenState) (_s : String) : Unit := ()

def LEAN_KWS : Array String := #[
  "def","fun","let","in","if","then","else","match","with","do","return","where",
  "have","show","from","by","class","instance","structure","inductive","namespace",
  "end","open","import","theorem","lemma","variable","universe","abbrev",
  "opaque","partial","mutual","private","protected","section","attribute",
  "for","while","repeat","at","try","catch","throw","macro","syntax","tactic",
  "set_option","derive","deriving","extends","override"]

def sanitize (name : String) : String :=
  if LEAN_KWS.any (· == name) then "«" ++ name ++ "»"
  else name.replace "/" "_"

def fmtTPs (params : Array String) : String :=
  if params.size == 0 then ""
  else " {" ++ String.intercalate "} {" (params.toList.map (fun p => p ++ " : Type")) ++ "}"

def fmtExplicitTPs (params : Array String) : String :=
  if params.size == 0 then ""
  else " " ++ String.intercalate " " (params.toList.map (fun p => "(" ++ p ++ " : Type)"))

partial def needsDoWrapping (body : IRExpr) : Bool :=
  body.tag == "Bind" || body.tag == "Await" || body.tag == "Sequence" ||
  (body.tag == "Let" && needsDoWrapping { tag := body.body })

def TS_API_TYPES : Array String := #[]

def sorryForType (t : String) : String :=
  if t.isEmpty then "sorry"
  else match t with
    | "Bool" => "(sorry : Bool)" | "String" => "(sorry : String)"
    | "Nat" => "(sorry : Nat)" | "Int" => "(sorry : Int)"
    | "Float" => "(sorry : Float)" | "Unit" => "()"
    | "Option" => "none" | "Array" => "#[]"
    | _ => "sorry"

partial def defaultForType (t : IRType) : String :=
  match t with
  | .Nat => "0" | .Int => "0" | .Float => "(0 : Float)"
  | .String => "\"\"" | .Bool => "false" | .Unit => "()"
  | .Array _ => "#[]" | .Option _ => "none"
  | .Tuple elems => if elems.size == 0 then "()"
    else "(" ++ String.intercalate ", " (elems.toList.map defaultForType) ++ ")"
  | _ => "default"

def isSimpleValue (s : String) : Bool :=
  let t := s.trimLeft
  t.startsWith "\"" || t == "true" || t == "false" || t == "default" || t == "none" || t == "#[]"

opaque collectFieldAccesses_impl (expr : IRExpr) (varName : String) : AssocMap String IRType
def collectFieldAccesses (expr : IRExpr) (varName : String) : AssocMap String IRType := collectFieldAccesses_impl expr varName

def isEmptyElse (e : IRExpr) : Bool :=
  e.tag == "LitUnit" || (e.tag == "Pure_" && e.value == "LitUnit")

def looksMonadic (s : String) : Bool :=
  let t := s.trimLeft
  t.startsWith "do" || t.startsWith "pure " || t.startsWith "return " ||
  t.startsWith "let " || t == "()" || t == "default"

def needsParens (e : IRExpr) : Bool :=
  e.tag == "App" || e.tag == "BinOp" || e.tag == "UnOp" || e.tag == "IfThenElse" ||
  e.tag == "Lambda" || e.tag == "Let" || e.tag == "LitFloat"

def fixStateEffect (eff : Effect) (params : Array IRParam) : Effect :=
  let selfParam := params.find? (fun p => p.name == "self")
  match selfParam with
  | none => eff
  | some sp => match eff with
    | .State st => if st == IRType.Unit then Effect.State sp.type else eff
    | _ => eff

partial def bodyContainsAnyVarRef (expr : IRExpr) (names : Array String) (_selfName : String) : Bool :=
  names.any (fun n => expr.name == n || expr.fn == n || expr.obj == n)

partial def bodyContainsVarRef (expr : IRExpr) (name : String) : Bool :=
  expr.name == name || expr.fn == name || expr.obj == name

opaque groupMutual_impl (decls : Array IRDecl) : Array (Array IRDecl)
def groupMutual (decls : Array IRDecl) : Array (Array IRDecl) := groupMutual_impl decls

partial def flattenConcat (e : IRExpr) : Option (Array IRExpr) :=
  if e.tag == "LitString" then some #[e]
  else if e.tag == "BinOp" && (e.op == "Concat" || e.op == "Add") then some #[e]
  else none

partial def isSafeInterp (e : IRExpr) : Bool :=
  e.tag == "LitString" || e.tag == "LitNat" || e.tag == "LitFloat" ||
  e.tag == "LitBool" || e.tag == "Var" || e.tag == "FieldAccess"

end TSLean.Generated.SelfHost.CodegenIndex
