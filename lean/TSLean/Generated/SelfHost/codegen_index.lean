-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/src/codegen/index.ts

import TSLean.Generated.SelfHost.Prelude
import TSLean.Generated.SelfHost.ir_types
import TSLean.DurableObjects.Http
import TSLean.DurableObjects.Model
import TSLean.DurableObjects.State
import TSLean.DurableObjects.Storage
import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions
import TSLean.Runtime.Monad
import TSLean.Stdlib.HashMap

open TSLean TSLean.Generated.Types TSLean.Stdlib.HashMap TSLean.DO

namespace TSLean.Generated.SelfHost.CodegenIndex

def GENERATED_BANNER : String := "-- Auto-generated by ts-lean-transpiler"

def DEFAULT_DERIVING : String := "Repr, BEq, Inhabited"

/-- Generate Lean 4 source code from an IR module. -/
def generateLean (mod : IRModule) : String :=
  sorry

-- State for Gen
structure GenState where
  mk ::
  lines : Array String
  ind : Nat
  structFields : AssocMap String (Array String)
  classToState : AssocMap String String
  definedNames : Array String
  inMutualBlock : Bool
  mutualTypeNames : Array String
  deriving Inhabited

def Gen.gen (self : GenState) (mod : IRModule) : String :=
  sorry /- Gen: body has sequential ifs outside do -/
def Gen.emitDeclsWithMutualDetection (self : GenState) (decls : Array IRDecl) : Unit :=
  sorry /- complex body -/
def Gen.collectDefinedNames (self : GenState) (decls : Array IRDecl) : Unit :=
  default

def Gen.collectStructInfo (self : GenState) (decls : Array IRDecl) : Unit :=
  default

def Gen.typeToLean (self : GenState) (t : IRType) (parens : Bool := false) : String :=
  sorry /- typeToLean: wraps irTypeToLean -/
def Gen.resolveType (self : GenState) (ty : String) : String :=
  ty /- resolveType: simplified -/

def Gen.inferTypeForDefault (self : GenState) (varName : String) : Option String :=
  default

def Gen.emitMissingStateStructs (self : GenState) (decls : Array IRDecl) : Unit :=
  sorry /- complex body -/
def Gen.emitSmartImports (self : GenState) (mod : IRModule) : Unit :=
  default

def Gen.collectImportNeeds (self : GenState) (d : IRDecl) (needs : Array String) : Unit :=
  default

def Gen.collectTypeImportNeeds (self : GenState) (t : IRType) (needs : Array String) : Unit :=
  default

def Gen.collectExprImportNeeds (self : GenState) (e : IRExpr) (needs : Array String) : Unit :=
  default

def Gen.emitDecl (self : GenState) (d : IRDecl) : Unit :=
  sorry /- match d.tag -/ 
-- (match on tag removed — patterns handled by sorry above)

def Gen.emitSection (self : GenState) (d : String) : Unit :=
  default

def Gen.emitStruct (self : GenState) (d : IRDecl) : Unit :=
  sorry /- emitStruct: complex body -/
def Gen.emitInductive (self : GenState) (d : String) : Unit :=
  default

def Gen.emitTypeAlias (self : GenState) (d : String) : Unit :=
  default

def Gen.emitFunc (self : GenState) (d : IRDecl) : Unit :=
  sorry /- emitFunc: complex body -/
def Gen.retSig (self : GenState) (eff : Effect) (retLean : String) : String :=
  sorry /- Gen: body has sequential ifs outside do -/
def Gen.fmtParam (self : GenState) (p : IRParam) (eff : Effect) : String :=
  sorry /- Gen: body has sequential ifs outside do -/
def Gen.emitVarDecl (self : GenState) (d : IRDecl) : Unit :=
  sorry /- emitVarDecl: complex body -/
def Gen.emitInstance (self : GenState) (d : IRDecl) : Unit :=
  sorry /- Gen: uses .tag on inductive -/
def Gen.emitTheorem (self : GenState) (d : IRDecl) : Unit :=
  sorry /- emitTheorem: complex body -/
def Gen.emitClass (self : GenState) (d : IRDecl) : Unit :=
  sorry /- emitClass: complex body -/
def Gen.emitNamespace (self : GenState) (d : String) : Unit :=
  default

def Gen.genExpr (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat := 0) : String :=
  if e.tag == "" then
      "default"
    else
      let indent : String := sorry
      default

def Gen._genExprInner (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (indent : String) : String :=
  sorry /- complex body -/
def Gen.genP (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String :=
  sorry /- Gen.genP: let-then-match/if pattern -/
def Gen.genMatch (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String :=
  sorry /- Gen: complex do body -/
def Gen.genPat (self : GenState) (p : IRPattern) : String :=
  sorry /- match p.tag -/ 
-- (match on tag removed — patterns handled by sorry above)

def Gen.genDoBlock (self : GenState) (stmts : Array DoStmt) (ctx : Effect) (depth : Nat) : String :=
  default

def Gen.genDoSeq (self : GenState) (stmts : Array IRExpr) (ctx : Effect) (depth : Nat) : String :=
  sorry /- genDoSeq: complex -/ /- Gen: complex do body -/
def Gen.chainSequentialIfs (self : GenState) (stmts : Array IRExpr) : Array IRExpr :=
  sorry /- Gen: complex do body -/
def Gen.genBinOp (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : String :=
  sorry /- Gen: body has sequential ifs outside do -/
def Gen.trySInterp (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) : Option String :=
  sorry /- Gen: body has sequential ifs outside do -/
def Gen.tryOptionMatch (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (indent : String) : Option String :=
  sorry /- Gen: complex do body -/
def Gen.genExprWithVarSubst (self : GenState) (e : IRExpr) (ctx : Effect) (depth : Nat) (from_ : String) (to : String) : String :=
  sorry /- Gen: complex do body -/
def Gen.emitComment (self : GenState) (c : String) : Unit :=
  sorry /- emitComment: emit "-- " ++ c -/ /- Gen: complex do body -/
def Gen.emit (self : GenState) (s : String) : Unit :=
  sorry /- emit: append to lines -/ /- Gen: complex do body -/
def LEAN_KWS : Array String := #[]

def sanitize (name : String) : String :=
  if LEAN_KWS.contains name then
      s!"«{name}»"
    else
      name.replace "/[^a-zA-Z0-9_.!?']/g" "_"

/-- Format type parameters as implicit ({T : Type}) — used for function definitions. -/
def fmtTPs (params : Array String) : String :=
  if params.size == 0 then
      ""
    else
      (" {" ++ (String.intercalate "} {" (params.toList.map (fun p => s!"{p} : Type")))) ++ "}"

/-- Format type parameters as explicit ((T : TSAny)) — used for structures and inductives. Explicit params let Lean infer type args at use sites without `(T := ...)` annotations. -/
def fmtExplicitTPs (params : Array String) : String :=
  if params.size == 0 then
      ""
    else
      " " ++ (String.intercalate " " (params.toList.map (fun p => s!"({p} : Type)")))

/-- Determine if an expression body needs `do` wrapping when inside an effectful function. Returns true when the body contains monadic operations (Bind/Await/Sequence with effects) that aren't already inside a DoBlock. -/
partial def needsDoWrapping (body : IRExpr) : Bool :=
  sorry /- match body.tag -/ 
-- (match on tag removed — patterns handled by sorry above)

def isSimpleValue (s : String) : Bool :=
  let t := s.trimLeft
  t.startsWith "\"" || t == "true" || t == "false" || t == "default" || t == "none" || t == "#[]"

/-- Collect all FIELD names (not method calls) accessed on a given variable. self.field → collected; self.method() → NOT collected (it's a method call). -/
def collectFieldAccesses (expr : IRExpr) (varName : String) : AssocMap String IRType :=
  default

/-- Check if an else branch is "empty" (LitUnit, pure (), or trivial). -/
def isEmptyElse (e : IRExpr) : Bool :=
  sorry /- isEmptyElse: body has sequential ifs outside do -/
def looksMonadic (s : String) : Bool :=
  sorry /- dangling let before end -/
-- (match on tag removed — patterns handled by sorry above)

end TSLean.Generated.SelfHost.CodegenIndex