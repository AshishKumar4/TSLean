-- Auto-generated by ts-lean-transpiler (fixed)
-- Source: tests/fixtures/basic/interfaces.ts

namespace TSLean.Generated.Basic.Interfaces

-- Interfaces → Lean structures
structure Point where
  x : Float
  y : Float
  deriving Repr, BEq, Inhabited

structure Rectangle where
  topLeft : Point
  bottomRight : Point
  deriving Repr, BEq, Inhabited

structure Named where
  name : String
  description : Option String := none
  deriving Repr, BEq, Inhabited

def distance (p1 : Point) (p2 : Point) : Float :=
  let dx := p2.x - p1.x
  let dy := p2.y - p1.y
  Float.sqrt (dx * dx + dy * dy)

def area (r : Rectangle) : Float :=
  (r.bottomRight.x - r.topLeft.x) * (r.bottomRight.y - r.topLeft.y)

def makePoint (x y : Float) : Point := { x, y }

end TSLean.Generated.Basic.Interfaces
