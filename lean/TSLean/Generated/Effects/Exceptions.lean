-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/tests/fixtures/effects/exceptions.ts

import TSLean.Runtime.Basic
import TSLean.Runtime.Monad

open TSLean

namespace TSLean.Generated.Exceptions

def ValidationError.init (self : ValidationError) (field : String) (reason : String) : Unit :=
  sorry

def parseAge (input : String) : ExceptT String IO Float :=
  let n : Float := String.toInt? input 10
  if Float.isNaN n then
    throw (TSError.mk "age" "must be a number")
  else
    if (n < 0) || (n > 150) then
      throw (TSError.mk "age" "must be between 0 and 150")
    else
      return n

def divide (a : Float) (b : Float) : ExceptT String IO Float :=
  if b == 0 then
    throw (TSError.mk "Division by zero")
  else
    return a / b

def safeDivide (a : Float) (b : Float) : Option Float :=
  tryCatch divide a b (fun _e => none)

def validateEmail (email : String) : ExceptT String IO String :=
  if !(email.includes "@") then
    throw (TSError.mk "email" "must contain @")
  else
    if email.length < 5 then
      throw (TSError.mk "email" "too short")
    else
      return email.toLowerCase.trim

end TSLean.Generated.Exceptions