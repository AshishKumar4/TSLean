-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/tests/fixtures/generics/branded-types.ts

import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions

open TSLean

namespace TSLean.Generated.BrandedTypes

structure UserId where
  mk ::
  val : String
  deriving Repr, BEq, Inhabited

structure RoomId where
  mk ::
  val : String
  deriving Repr, BEq, Inhabited

structure MessageId where
  mk ::
  val : String
  deriving Repr, BEq, Inhabited

structure SessionToken where
  mk ::
  val : String
  deriving Repr, BEq, Inhabited

structure EmailAddress where
  mk ::
  val : String
  deriving Repr, BEq, Inhabited

def makeUserId (raw : String) : UserId :=
  UserId.mk raw

def makeRoomId (raw : String) : RoomId :=
  RoomId.mk raw

def makeSessionToken (raw : String) : SessionToken :=
  SessionToken.mk raw

def getUserIdString (id : UserId) : String :=
  id.val

structure UserProfile where
  mk ::
  id : UserId
  email : EmailAddress
  displayName : String
  deriving Repr, BEq, Inhabited

def createUserProfile (id : UserId) (email : EmailAddress) (name : String) : UserProfile :=
  { id := id, email := email, displayName := name }

end TSLean.Generated.BrandedTypes