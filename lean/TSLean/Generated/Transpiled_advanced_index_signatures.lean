-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/tests/fixtures/advanced/index-signatures.ts

import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions
import TSLean.Stdlib.HashMap
set_option linter.unusedVariables false

open TSLean TSLean.Stdlib.HashMap

namespace TSLean.Transpiled.IndexSignatures

-- // Index signatures and mapped types
abbrev StringMap := AssocMap String String

structure NumberDict where
  mk ::
  size : Float
  deriving Repr, BEq, Inhabited

structure Env where
  mk ::
  NODE_ENV : String
  deriving Repr, BEq, Inhabited

def getFromMap (m : StringMap) (key : String) : Option String :=
  m.getD key default

def setInMap (m : StringMap) (key : String) (value : String) : StringMap :=
  m

-- -- mapped type: { [K in keyof T]: T[K] | null }
abbrev Nullable {T : Type} := String

-- -- mapped type: { [K in keyof T]?: T[K] }
abbrev Optional {T : Type} := String

-- -- mapped type: { readonly [K in keyof T]: T[K] }
abbrev Readonly_ {T : Type} := String

end TSLean.Transpiled.IndexSignatures