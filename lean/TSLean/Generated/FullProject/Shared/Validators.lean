-- Auto-generated by ts-lean-transpiler
-- Source: /workspace/tslean/tests/fixtures/full-project/shared/validators.ts

import TSLean.Runtime.Basic
import TSLean.Runtime.Coercions

open TSLean

namespace TSLean.Generated.Validators

structure ValidationResult where
  mk ::
  valid : Bool
  errors : Array String
  deriving Repr, BEq

def validateEmail (email : String) : ValidationResult :=
  let errors : Array String := #[]
    if !(email.includes "@") then
      errors.push "must contain @"
    else
      ()
    if email.length < 5 then
      errors.push "too short"
    else
      ()
    if email.length > 254 then
      errors.push "too long"
    else
      ()
    { valid := errors.size == 0, errors := errors }

def validateDisplayName (name : String) : ValidationResult :=
  let errors : Array String := #[]
    if name.length < 2 then
      errors.push "too short"
    else
      ()
    if name.length > 50 then
      errors.push "too long"
    else
      ()
    { valid := errors.size == 0, errors := errors }

def validateRoomName (name : String) : ValidationResult :=
  let errors : Array String := #[]
    if name.length < 3 then
      errors.push "too short"
    else
      ()
    if name.length > 100 then
      errors.push "too long"
    else
      ()
    { valid := errors.size == 0, errors := errors }

end TSLean.Generated.Validators