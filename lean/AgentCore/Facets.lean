/-
Bounded context barrel for `packages/agent-core/src/facets` (SPEC §4, §7, §11).
Mirrors the context's own `index.ts`: one import per emitted file, nothing else.
-/
import AgentCore.Facets.Enforcement
