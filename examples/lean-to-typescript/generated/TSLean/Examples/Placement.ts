/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v6
 * Lean module: TSLean.Examples.Placement
 * Generated module: TSLean/Examples/Placement.ts
 * Package entry module: TSLean.Examples.Placement
 * Semantic identity: sha256:d8838809bef9778bcd68c2321abd4159d884408a80517a18c2f50293fe6825c0
 * Input closure: sha256:d56c29a94e52b827fd835c3d8036afa4580af41e261cd354e70d4a46317892d8
 * Semantic IR: sha256:ce0c5336f029db926f73ccfae9224041318e839b9555862894c613af98c2330d
 * Generated module body: sha256:4028f0223d4538518425e8bfe2108d9e72969a6ef0798bc7f1ab2b8b00e0e794
 * Generated package body: sha256:4637b8de80d0af1483710cca9a62a97a005f51a0f2ee6bab77be219e28be0e4a
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:cdfe1c19c5d522c74a23029794202e6f09fcdc12ae80fd10d1b3fe83faf19dba
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
export type Placement = "bundled" | "provider" | "dynamic";

export interface PlacementSetInit {
    readonly bundled: boolean;
    readonly provider: boolean;
    readonly dynamic: boolean;
}

export interface PlacementSetData {
    readonly bundled: boolean;
    readonly provider: boolean;
    readonly dynamic: boolean;
}

export class PlacementSet {
    public readonly bundled: boolean;
    public readonly provider: boolean;
    public readonly dynamic: boolean;
    public constructor(init: PlacementSetInit) {
        this.bundled = init.bundled;
        this.provider = init.provider;
        this.dynamic = init.dynamic;
        Object.freeze(this);
    }
    public static fromData(value: GeneratedData): PlacementSet {
        const data = requireDataFields(value, "PlacementSet", ["bundled", "provider", "dynamic"]);
        return new PlacementSet({
            bundled: requireBoolean(data["bundled"], "PlacementSet bundled"),
            provider: requireBoolean(data["provider"], "PlacementSet provider"),
            dynamic: requireBoolean(data["dynamic"], "PlacementSet dynamic")
        });
    }
    public intersect(right: PlacementSet): PlacementSet {
        return new PlacementSet({
            bundled: this.bundled && right.bundled,
            provider: this.provider && right.provider,
            dynamic: this.dynamic && right.dynamic
        });
    }
    public toData(): PlacementSetData {
        return {
            bundled: this.bundled,
            provider: this.provider,
            dynamic: this.dynamic
        };
    }
    public equals(other: PlacementSet): boolean {
        return this.bundled === other.bundled && this.provider === other.provider && this.dynamic === other.dynamic;
    }
}

export function choosePlacement(manifest: PlacementSet, policy: PlacementSet, substrate: PlacementSet, trust: PlacementSet): Option<Placement> {
    const available = manifest.intersect(policy).intersect(substrate).intersect(trust);
    if (available.dynamic) {
        return { kind: "some", value: "dynamic" };
    }
    if (available.provider) {
        return { kind: "some", value: "provider" };
    }
    if (available.bundled) {
        return { kind: "some", value: "bundled" };
    }
    return { kind: "none" };
}

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

export type Option<A> = {
    readonly kind: "none";
} | {
    readonly kind: "some";
    readonly value: A;
};

function isDataObject(value: GeneratedData): value is {
    readonly [key: string]: GeneratedData;
} {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

function requireDataFields(value: GeneratedData, name: string, fields: readonly string[]): {
    readonly [key: string]: GeneratedData;
} {
    if (!isDataObject(value)) {
        throw new TypeError(`${name} data must be an object`);
    }
    if (Object.keys(value).length !== fields.length || fields.some((field: string) => !Object.hasOwn(value, field))) {
        throw new TypeError(`${name} data fields must be exactly ${fields.join(", ")}`);
    }
    return value;
}

function requireBoolean(value: GeneratedData, name: string): boolean {
    if (value === true || value === false) {
        return value;
    }
    throw new TypeError(`${name} must be a boolean`);
}
