/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v6
 * Lean module: TSLean.Examples.Roundtrip.Flags
 * Generated module: TSLean/Examples/Roundtrip/Flags.ts
 * Package entry module: TSLean.Examples.Roundtrip.Flags
 * Semantic identity: sha256:d3b8067b39f367749a5ca925221fab0e07a2693f2b10848e0a6bdc5dac6dfde4
 * Input closure: sha256:fd594f23881f33ddd30f16972fb4aff31ee9a97f5cd786e6890b3a3c30cb1ec4
 * Semantic IR: sha256:b5b551c503d5f21b040233c4297b472dbfbbc2a476f532bc1c5ef73e331e3d30
 * Generated module body: sha256:8e6581f88d3c37524e420cb68f45c8932b76451d579be2a5aa9167160cd7c160
 * Generated package body: sha256:2b35b600144b6f2a5a183b00d547629b62d16ea83957e06af58c80559a3f2e10
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:ae5bddad3e3bc2f92054fb4764d7bdc92b1a34306a01c452236eb597ad785c38
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
export interface FlagsInit {
    readonly alpha: boolean;
    readonly beta: boolean;
    readonly gamma: boolean;
}

export interface FlagsData {
    readonly alpha: boolean;
    readonly beta: boolean;
    readonly gamma: boolean;
}

/**
 * Three independent switches.
 */
export class Flags {
    public readonly alpha: boolean;
    public readonly beta: boolean;
    public readonly gamma: boolean;
    public constructor(init: FlagsInit) {
        this.alpha = init.alpha;
        this.beta = init.beta;
        this.gamma = init.gamma;
        Object.freeze(this);
    }
    public static fromData(value: GeneratedData): Flags {
        const data = requireDataFields(value, "Flags", ["alpha", "beta", "gamma"]);
        return new Flags({
            alpha: requireBoolean(data["alpha"], "Flags alpha"),
            beta: requireBoolean(data["beta"], "Flags beta"),
            gamma: requireBoolean(data["gamma"], "Flags gamma")
        });
    }
    /**
     * Only the switches both sides have on.
     */
    public meet(right: Flags): Flags {
        return new Flags({
            alpha: this.alpha && right.alpha,
            beta: this.beta && right.beta,
            gamma: this.gamma && right.gamma
        });
    }
    /**
     * Every switch either side has on.
     */
    public union(right: Flags): Flags {
        return new Flags({
            alpha: this.alpha || right.alpha,
            beta: this.beta || right.beta,
            gamma: this.gamma || right.gamma
        });
    }
    public toData(): FlagsData {
        return {
            alpha: this.alpha,
            beta: this.beta,
            gamma: this.gamma
        };
    }
    public equals(other: Flags): boolean {
        return this.alpha === other.alpha && this.beta === other.beta && this.gamma === other.gamma;
    }
}

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

function isDataObject(value: GeneratedData): value is {
    readonly [key: string]: GeneratedData;
} {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

function requireDataFields(value: GeneratedData, name: string, fields: readonly string[]): {
    readonly [key: string]: GeneratedData;
} {
    if (!isDataObject(value)) {
        throw new TypeError(`${name} data must be an object`);
    }
    if (Object.keys(value).length !== fields.length || fields.some((field: string) => !Object.hasOwn(value, field))) {
        throw new TypeError(`${name} data fields must be exactly ${fields.join(", ")}`);
    }
    return value;
}

function requireBoolean(value: GeneratedData, name: string): boolean {
    if (value === true || value === false) {
        return value;
    }
    throw new TypeError(`${name} must be a boolean`);
}
