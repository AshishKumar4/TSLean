/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v5
 * Lean module: TSLean.Examples.Roundtrip.Bits
 * Generated module: TSLean/Examples/Roundtrip/Bits.ts
 * Package entry module: TSLean.Examples.Roundtrip.Bits
 * Semantic identity: sha256:8b07e1fd6c94dca621c36e26acb7ea18681e7ea8897b9c5d2ea647cec6f453f4
 * Input closure: sha256:6a3f4fbbd9476325c18848b3c9faf350d12dff3fabdf5ad038dcf76f26a7f0c2
 * Semantic IR: sha256:1eca28fc5cc32e6964dd41960bbba36ff528afbf0c2e901b4c735126f49b6fac
 * Generated module body: sha256:0c5b4d7ad8bc770c3b69861047d67d2cece78e2396faf36de125b8a037bd9a8a
 * Generated package body: sha256:0a33011386f717b3632558b1c75f7516f2a16d6b639c90e8a5b3b49cba77d920
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:41abb02d25ec814cf4a133d27a59509bbff9a8627a7f805c3615356877ad21c2
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
export interface BitsInit {
    readonly low: boolean;
    readonly middle: boolean;
    readonly high: boolean;
}

export interface BitsData {
    readonly low: boolean;
    readonly middle: boolean;
    readonly high: boolean;
}

/**
 * Three independent bits.
 */
export class Bits {
    public readonly low: boolean;
    public readonly middle: boolean;
    public readonly high: boolean;
    public constructor(init: BitsInit) {
        this.low = init.low;
        this.middle = init.middle;
        this.high = init.high;
        Object.freeze(this);
    }
    public static fromData(value: GeneratedData): Bits {
        const data = dataFields(value, "Bits", ["low", "middle", "high"]);
        return new Bits({
            low: requireBoolean(data["low"], "Bits low"),
            middle: requireBoolean(data["middle"], "Bits middle"),
            high: requireBoolean(data["high"], "Bits high")
        });
    }
    /**
     * Whether at least one bit is set.
     */
    public anySet(): boolean {
        return this.low || this.middle || this.high;
    }
    /**
     * The bitwise exclusive disjunction of two triples.
     */
    public exclusive(right: Bits): Bits {
        return new Bits({
            low: (this.low || right.low) && !(this.low && right.low),
            middle: (this.middle || right.middle) && !(this.middle && right.middle),
            high: (this.high || right.high) && !(this.high && right.high)
        });
    }
    public toData(): BitsData {
        return {
            low: this.low,
            middle: this.middle,
            high: this.high
        };
    }
    public equals(other: Bits): boolean {
        return this.low === other.low && this.middle === other.middle && this.high === other.high;
    }
}

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

function isDataObject(value: GeneratedData): value is {
    readonly [key: string]: GeneratedData;
} {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

function dataFields(value: GeneratedData, name: string, fields: readonly string[]): {
    readonly [key: string]: GeneratedData;
} {
    if (!isDataObject(value)) {
        throw new TypeError(`${name} data must be an object`);
    }
    if (Object.keys(value).length !== fields.length || fields.some((field: string) => !Object.hasOwn(value, field))) {
        throw new TypeError(`${name} data fields must be exactly ${fields.join(", ")}`);
    }
    return value;
}

function requireBoolean(value: GeneratedData, name: string): boolean {
    if (value === true || value === false) {
        return value;
    }
    throw new TypeError(`${name} must be a boolean`);
}
