/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v6
 * Lean module: TSLean.Examples.Roundtrip.Access
 * Generated module: TSLean/Examples/Roundtrip/Access.ts
 * Package entry module: TSLean.Examples.Roundtrip.Access
 * Semantic identity: sha256:90e9ffc07e6a6607d6d8fdbb81d8a5488a487e3878a003fd91ffa1cb7a6cd926
 * Input closure: sha256:d5ba7ebbfd45f0f191098181ec5752f866370646da6c7e590d3f35c41314d56c
 * Semantic IR: sha256:7e21bdc5ef07e5a54eb4d003a3f96a4b00c7f40c217ba5843512651609e5a06e
 * Generated module body: sha256:d082375c0ace21afa90b49285e34019bed74da189ca89e4a04bbf70f5f481c15
 * Generated package body: sha256:7482ea6c3f8a4dea291c46786cad7de9a1af7441c1d79a3b879907fb29dd63f4
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:ad89adcd4348a0450675135c1abbc9bf6eff606a61dbde87a947f3394e890724
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
/**
 * What a subject may do.
 */
export type Action = "read" | "write" | "erase";

export interface PermissionsInit {
    readonly reader: boolean;
    readonly writer: boolean;
    readonly owner: boolean;
}

export interface PermissionsData {
    readonly reader: boolean;
    readonly writer: boolean;
    readonly owner: boolean;
}

/**
 * The permissions one subject holds.
 */
export class Permissions {
    public readonly reader: boolean;
    public readonly writer: boolean;
    public readonly owner: boolean;
    public constructor(init: PermissionsInit) {
        this.reader = init.reader;
        this.writer = init.writer;
        this.owner = init.owner;
        Object.freeze(this);
    }
    public static fromData(value: GeneratedData): Permissions {
        const data = requireDataFields(value, "Permissions", ["reader", "writer", "owner"]);
        return new Permissions({
            reader: requireBoolean(data["reader"], "Permissions reader"),
            writer: requireBoolean(data["writer"], "Permissions writer"),
            owner: requireBoolean(data["owner"], "Permissions owner")
        });
    }
    /**
     * Whether the permissions admit the action.
     */
    public admits(action: Action): boolean {
        if (action === "read") {
            return this.reader || this.writer || this.owner;
        }
        if (action === "write") {
            return this.writer || this.owner;
        }
        return this.owner;
    }
    public toData(): PermissionsData {
        return {
            reader: this.reader,
            writer: this.writer,
            owner: this.owner
        };
    }
    public equals(other: Permissions): boolean {
        return this.reader === other.reader && this.writer === other.writer && this.owner === other.owner;
    }
}

function requireAction(value: GeneratedData, name: string): Action {
    if (value === "read" || value === "write" || value === "erase") {
        return value;
    }
    throw new TypeError(`${name} must name a Action`);
}

export const Action = Object.freeze({
    fromData(value: GeneratedData): Action {
        return requireAction(value, "Action");
    }
});

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

function isDataObject(value: GeneratedData): value is {
    readonly [key: string]: GeneratedData;
} {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

function requireDataFields(value: GeneratedData, name: string, fields: readonly string[]): {
    readonly [key: string]: GeneratedData;
} {
    if (!isDataObject(value)) {
        throw new TypeError(`${name} data must be an object`);
    }
    if (Object.keys(value).length !== fields.length || fields.some((field: string) => !Object.hasOwn(value, field))) {
        throw new TypeError(`${name} data fields must be exactly ${fields.join(", ")}`);
    }
    return value;
}

function requireBoolean(value: GeneratedData, name: string): boolean {
    if (value === true || value === false) {
        return value;
    }
    throw new TypeError(`${name} must be a boolean`);
}
