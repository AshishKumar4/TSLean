/*
 * Generated by TSLean from kernel-checked Lean declarations.
 * Fragment: tslean-semantic-typed-v6
 * Lean module: TSLean.Examples.Roundtrip.Decisions
 * Generated module: TSLean/Examples/Roundtrip/Decisions.ts
 * Package entry module: TSLean.Examples.Roundtrip.Decisions
 * Semantic identity: sha256:fa63106ec968ac534632666dc8131f13256bef6e2ee27befe5af23a3e5381b6f
 * Input closure: sha256:ad036df1ebce7cf4f08e8a5a6251520233091361e17ff7e21bfa46a2299226b0
 * Semantic IR: sha256:a9963fe12ae348bdca03a9164d114e16979fa576d09d4f9959add8448e7e8bcf
 * Generated module body: sha256:dae396bc7c1927f7170f94f8520c52e93c62aa38bf694d33139db7b18252a98d
 * Generated package body: sha256:e2ecd1e2244d67abc6e2472a15f28b4adc9f0b0107ba29339e98b2df37cf0339
 * Lean toolchain: leanprover/lean4:v4.33.1
 * Lean: Lean (version 4.33.1, x86_64-unknown-linux-gnu, commit 819816b2e0a3bf405af45ae5c7af2491d8f5bee6, Release)
 * Lake: Lake version 5.0.0-src+819816b (Lean version 4.33.1)
 * Runtime certificates: sha256:ad89adcd4348a0450675135c1abbc9bf6eff606a61dbde87a947f3394e890724
 * Environment attestation: recorded in the manifest sidecar; it does not affect these bytes.
 * Evidence boundary: Lean certificates prove source-to-model refinement conditionally; engine conformance remains an explicit obligation.
 */
/**
 * Whether a request carried a credential.
 */
export type Credential = "present" | "absent";

/**
 * Which way a request was routed.
 */
export type Lane = "fast" | "slow";

/**
 * One request, as the two decisions it carries.
 */
export interface Request {
    readonly lane: Lane;
    readonly credential: Credential;
}

/**
 * A match on two discriminants, decided lexicographically in Lean's own arm order. The last arm is a
 * pair of wildcards, so the expansion places it on every combination the earlier arms do not accept.
 */
export function admits(lane: Lane, credential: Credential): boolean {
    if (lane === "fast") {
        if (credential === "present") {
            return true;
        }
        return false;
    }
    if (credential === "present") {
        return false;
    }
    return false;
}

/**
 * A match on two discriminants that are field reads rather than binders. A field read is
 * re-readable, which is what the expansion requires of every discriminant it reads again.
 */
export function decide(request: Request): boolean {
    const value = request.lane;
    if (value === "fast") {
        const value$2 = request.credential;
        if (value$2 === "present") {
            return true;
        }
        return false;
    }
    const value$3 = request.credential;
    if (value$3 === "present") {
        return false;
    }
    return false;
}

/**
 * A `let` inside an argument, hoisted to a `const` in front of the call it was an operand of. The
 * sibling operand is a binder read, which the hoist may step over.
 */
export function hoisted(lane: Lane, allowed: boolean): boolean {
    const fast = lane === "fast" ? true : false;
    return fast || allowed;
}

/**
 * A dependent match: the alternative binds the discriminant equation, which is a proof and so has
 * no runtime image.
 */
export function weight(lane: Lane): Credential {
    if (lane === "fast") {
        return "present";
    }
    return "absent";
}

function requireCredential(value: GeneratedData, name: string): Credential {
    if (value === "present" || value === "absent") {
        return value;
    }
    throw new TypeError(`${name} must name a Credential`);
}

export const Credential = Object.freeze({
    fromData(value: GeneratedData): Credential {
        return requireCredential(value, "Credential");
    }
});

function requireLane(value: GeneratedData, name: string): Lane {
    if (value === "fast" || value === "slow") {
        return value;
    }
    throw new TypeError(`${name} must name a Lane`);
}

export const Lane = Object.freeze({
    fromData(value: GeneratedData): Lane {
        return requireLane(value, "Lane");
    }
});

function requireRequest(value: GeneratedData, _name: string): Request {
    const data = requireDataFields(value, "Request", ["lane", "credential"]);
    return {
        lane: requireLane(data["lane"], "Request lane"),
        credential: requireCredential(data["credential"], "Request credential")
    };
}

export const Request = Object.freeze({
    fromData(value: GeneratedData): Request {
        return requireRequest(value, "Request");
    }
});

export type GeneratedData = boolean | bigint | number | string | null | undefined | readonly GeneratedData[] | {
    readonly [key: string]: GeneratedData;
};

function isDataObject(value: GeneratedData): value is {
    readonly [key: string]: GeneratedData;
} {
    return typeof value === "object" && value !== null && !Array.isArray(value);
}

function requireDataFields(value: GeneratedData, name: string, fields: readonly string[]): {
    readonly [key: string]: GeneratedData;
} {
    if (!isDataObject(value)) {
        throw new TypeError(`${name} data must be an object`);
    }
    if (Object.keys(value).length !== fields.length || fields.some((field: string) => !Object.hasOwn(value, field))) {
        throw new TypeError(`${name} data fields must be exactly ${fields.join(", ")}`);
    }
    return value;
}

export function requireBoolean(value: GeneratedData, name: string): boolean {
    if (value === true || value === false) {
        return value;
    }
    throw new TypeError(`${name} must be a boolean`);
}
