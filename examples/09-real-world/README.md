# 09 — Real-World Patterns

Three realistic TypeScript files demonstrating combined features:

- **todo-api.ts** — Data structures, CRUD operations, discriminated unions, array operations
- **config-parser.ts** — Error handling, optional values, null coalescing, validation
- **event-system.ts** — Generics, Map collections, higher-order functions
